




% 1. 单位矩阵 mxn
eye(3,3);

% 2.魔方矩阵：
magic(5);

% 一、 矩阵的运算；

% 1. 转置；

a=magic(5);
a';

% 2. 矩阵的行列式： det （a） 

det(a);

% 3. 计算矩阵的逆；

inv(a);

% 4. 求解线性方程的时候：

    % 1. 求逆矩阵后再相乘： X = A-1 * B；
    
%   X = inv(a) * b;
    
    % 2. 直接左除 \（右除 /） 

%   X = A\B;

%  5. 矩阵的删除：

    %  2. 给某一行附空值；

%   a[3]
    
%   6. 附加，扩展：

    %  3. 

     